function BASOOP_GUI_STAGE1(defaults)
% Call GUI with the echoview mode.
% This function is just a shortcut to GUI(defaults, 'Echoview')
%
% The echoview mode contains the GUI settings for creating echoview
% worksheets through to exporting echointegration results.

if nargin < 1
    defaults = '?';
end

BASOOP_GUI(defaults, 'Echoview');